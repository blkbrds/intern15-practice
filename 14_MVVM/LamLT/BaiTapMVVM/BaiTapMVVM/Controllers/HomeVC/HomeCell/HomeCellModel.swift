import Foundation

class HomeCellModel {

    // MARK: - Properties
    var data: DataManager = DataManager()

    // MARK: - Init
    init(data: DataManager = DataManager()) {
        self.data = data
    }
}
